OPENAI_API_KEY present: True len: 36
Found .msg: 130
Parsed OK: 125 | Failed: 5
Combined chars: 837432
Chunks: 16 | first chunk chars: 55000

---------------------------------------------------------------------------
RuntimeError                              Traceback (most recent call last)
Cell In[19], line 232
    229 print("Combined chars:", stats["combined_chars"])
    231 client = build_client()
--> 232 final_summary = map_reduce_summary(client, combined_text)
    234 ts = datetime.now().strftime("%Y%m%d_%H%M%S")
    235 out_docx = OUT_DIR / f"mail_conversations_summary_{ts}.docx"

Cell In[19], line 150, in map_reduce_summary(client, full_text)
    148 combined = "\n\n".join(partials).strip()
    149 if not combined:
--> 150     raise RuntimeError("All map summaries returned empty.")
    152 final_prompt = (
    153     f"Combine these partial summaries into ONE coherent summary under {MAX_FINAL_WORDS} words.\n"
    154     "Output structure:\n"
   (...)    160     f"{combined}"
    161 )
    162 final = llm_summarize(client, final_prompt, max_completion_tokens=1200)

RuntimeError: All map summaries returned empty.
