from pathlib import Path
import json

# 1) Load the stats json you already produced
stats_path = r"C:\Users\SHTANDO\OneDrive - Mercedes-Benz (corpdir.onmicrosoft.com)\DWT_MP_RM1 - Dokumente\Project Chatbot\Available data\new data\mail_summaries\mail_conversations_summary_20260313_131718.json"
stats = json.loads(Path(stats_path).read_text(encoding="utf-8", errors="ignore"))
print("input_chars:", stats.get("input_chars"))
print("output_words:", stats.get("output_words"))
print("vision_enabled:", stats.get("vision_enabled"))

# 2) Read the markdown file (your code wrote it)
md_path = stats["files"]["md"]
md = Path(md_path).read_text(encoding="utf-8", errors="ignore")
print("\nMD length:", len(md))
print("MD preview:", repr(md[:500]))

# 3) If MD is only headings, your pipeline never inserted summary text
#    Show last 500 chars too:
print("\nMD tail preview:", repr(md[-500:]))
