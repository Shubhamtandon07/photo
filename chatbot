import subprocess

p = subprocess.run(["winget", "list"], capture_output=True, text=True)
print("rc:", p.returncode)
out = (p.stdout or "") + "\n" + (p.stderr or "")
hits = [line for line in out.splitlines() if "tesseract" in line.lower()]
print("hits:", hits[:50])
