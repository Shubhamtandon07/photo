from pathlib import Path

log = Path(r"C:\Users\SHTANDO\AppData\Local\Temp\winget_tesseract_install.log")
print("exists:", log.exists(), "size:", log.stat().st_size if log.exists() else None)

if log.exists():
    txt = log.read_text(errors="ignore")
    lines = txt.splitlines()
    tail = "\n".join(lines[-300:])
    print("\n=== LOG TAIL (last 300 lines) ===\n")
    print(tail)
