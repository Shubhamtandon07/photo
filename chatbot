---------------------------------------------------------------------------
RuntimeError                              Traceback (most recent call last)
Cell In[22], line 505
    501     return summary, stats
    504 # --- RUN (one-liner) ---
--> 505 summary, stats = run_summary()
    506 summary[:1200]  # preview

Cell In[22], line 462, in run_summary()
    459 failures_log.write_text("\n".join(failures) if failures else "(none)", encoding="utf-8")
    461 client = build_client()
--> 462 summary = map_reduce_summary(client, combined)
    464 out_docx = OUT_DIR / f"mail_conversations_summary_{ts}.docx"
    465 out_html = OUT_DIR / f"mail_conversations_summary_{ts}.html"

Cell In[22], line 359, in map_reduce_summary(client, full_text)
    357 final = llm_summarize(client, final_prompt, max_completion_tokens=1200).strip()
    358 if not final:
--> 359     raise RuntimeError("Final reduce step returned empty.")
    360 return final

RuntimeError: Final reduce step returned empty.
