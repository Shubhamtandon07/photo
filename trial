import sys, subprocess

cmd = [sys.executable, "-m", "pip", "uninstall", "-y", "huggingface-hub"]
p = subprocess.run(cmd, capture_output=True, text=True)
print("RETURN CODE:", p.returncode)
print("STDOUT:\n", p.stdout)
print("STDERR:\n", p.stderr)
