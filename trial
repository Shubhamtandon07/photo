r = requests.post(f"{LIBRE_URL}/api/translate", data=payload, timeout=60)
print("status:", r.status_code)
print("text:", r.text[:500])
if r.status_code == 200:
    print("translated:", r.json().get("translatedText"))
